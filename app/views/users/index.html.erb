<% provide(:title, 'Users') %>

<ul class="list-group">
    <% @users.each do |user| %>
        <li class="list-group-item">
            <%= gravatar_for user %>
            <%= link_to user.name, user_path(user) %>
        </li>
    <% end %>
</ul>